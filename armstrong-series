x,y=map(int,input().split())
for i in range(x,y+1):
    s=0
    b=str(i)
    for j in range(len(b)):
        a=i%10
        s=s+(a**len(b))
        i=i//10
    if int(b)==s:
        print(s,end=' ')

